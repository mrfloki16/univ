#!/bin/python3

# explanations
print("Adding integer x with y and round it with 2 digit!!")

# asking float to add
x = float(input("x = ? : "))
y = float(input("y = ? : "))

print(f"answer = {round(x+y,2)}")
